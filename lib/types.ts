export type RunningEvent = {
    id: number
    event_name: string
    distance: number
    location: string
    event_date: string
    event_time: string
    image_url: string
    description: string
}